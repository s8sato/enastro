export function formatLocalDateOnly(epochMs: number, offsetMinutes: number): string;

export function formatTzOffsetLabel(offsetMinutes: number): string;

export function formatTzAbbrev(offsetMinutes: number): string;

export function formatLocalTimestamp(epochMs: number, offsetMinutes: number): string;
